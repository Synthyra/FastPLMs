[pytest]
markers =
    slow: loads two models simultaneously (compliance tests)
    gpu: requires CUDA GPU
    large: requires 24+ GB VRAM (3B parameter models)
    structure: structure prediction models (Boltz2, ESMFold)
testpaths = testing
